# Screenshots / images

Placeholder directory for README imagery. Drop real PNGs here to replace the
placeholders referenced in the top-level `README.md`:

- `hero.png` — wide hero shot (editor with a couple of tiled panes + the
  command palette open). ~820px wide.
- `tiling.png` — tiling/splits close-up. ~400px wide.
- `agents.png` — the AI agent panel. ~400px wide.

Keep images reasonably compressed; prefer PNG for UI screenshots.
