//! umber-proto — workspace protocol types (core <-> backend).
//!
//! P-phase: **P1** (proto split lands with the local backend; same code path
//! carries the P3 SSH/`umberd` remote backend). Stub for the P0 render spike.
